server:
  port: 8761

spring:
  application:
    name: module-eureka-server
  security:
    user:
      name: user
      password: user

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
