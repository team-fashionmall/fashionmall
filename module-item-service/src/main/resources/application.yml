server:
  port: 8082

spring:
  application:
    name: module-item-service
  jpa:
    hibernate:
      ddl-auto: validate

  profiles:
    active: local

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true

    service-url:
      defaultZone: http://localhost:8761/eureka/