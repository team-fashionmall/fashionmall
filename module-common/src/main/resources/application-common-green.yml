spring:
  profiles:
    group:
      "green": "green_port"
---
spring:
  data:
    redis:
      host: localhost
      port: 6379

  config:
    activate:
      on-profile: "green_port"

jwt:
  secret:
    key: ${JWT_SECRET_KEY}