spring:
  profiles:
    group:
      blue: "blue"
---
spring:
  data:
    redis:
      host: localhost
      port: 6379

  config:
    activate:
      on-profile: "blue"

jwt:
  secret:
    key: ${{ secrets.JWT_SECRET_KEY }}